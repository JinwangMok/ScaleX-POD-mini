# Kyverno — policy engine for all clusters
# Deployed to common/ for consistent policy enforcement

admissionController:
  replicas: 1

backgroundController:
  replicas: 1

cleanupController:
  replicas: 1

reportsController:
  replicas: 1

# Override broken bitnami/kubectl:1.30.2 image (removed from Docker Hub)
webhooksCleanup:
  image:
    registry: docker.io
    repository: bitnami/kubectl
    tag: "1.31"

policyReportsCleanup:
  image:
    registry: docker.io
    repository: bitnami/kubectl
    tag: "1.31"
