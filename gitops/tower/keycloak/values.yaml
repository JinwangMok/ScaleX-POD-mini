auth:
  existingSecret: keycloak-admin
production: false
proxy: edge
httpRelativePath: /
service:
  type: ClusterIP
  ports:
    http: 80
postgresql:
  image:
    tag: 17.4.0-debian-12-r25
  auth:
    existingSecret: keycloak-db
    secretKeys:
      adminPasswordKey: password
      userPasswordKey: password
