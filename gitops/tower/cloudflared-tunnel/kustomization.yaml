apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: cloudflared
    repo: https://community-charts.github.io/helm-charts
    version: 2.1.2
    releaseName: cloudflared-tunnel
    namespace: kube-tunnel
    valuesFile: values.yaml
